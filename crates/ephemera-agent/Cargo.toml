[package]
name = "ephemera-agent"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "Reserved for the distributed node-agent daemon (multi-host Zyvor Ephemera deployments)"

[dependencies]
